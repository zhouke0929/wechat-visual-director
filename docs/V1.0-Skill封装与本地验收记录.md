# V1.0 Skill 封装与本地验收记录

- 状态：公开 Alpha 已发行；真实公众号核心链路验收通过，含图富文本复制待专项验收
- 日期：2026-07-23
- Skill 名称：`wechat-visual-director`
- 范围：本地安装、Agent 调用、双方案、人工评审、复制/交付包与可选 Wenyan 草稿适配器

## 1. 本轮结果

仓库已从“可以手动运行的前后端项目”变为“可被 Agent 调用的薄 Skill”。Skill 不复制视觉规则，也不生成整段 HTML；它约束宿主 Agent 产出规范化 Markdown，并通过统一 CLI 调用现有核心程序。

```text
用户主题 / 资料 / Markdown
→ 宿主 Agent 按 wechat_article.v1 整理
→ scripts/visual-director.ps1
→ CLI 自动启动服务、预检并生成双方案
→ Web 工作台人工确认
```

## 2. 发行结构

```text
SKILL.md                         # Git/本地 Skill 根入口
agents/openai.yaml               # Codex UI 元数据
references/article-protocol.md   # Agent 写作与结构约束
references/cli-contract.md       # JSON、状态和退出码
scripts/install.ps1              # Windows 幂等依赖安装
scripts/visual-director.ps1      # 稳定 CLI 包装入口
scripts/persistent-launcher.ps1  # 固定安装目录入口与数据/配置环境注入
apps/api                         # 核心 API 与 CLI
apps/web                         # 本地人工评审工作台
```

OpenClaw 当前 Git 与本地目录安装均要求 `SKILL.md` 位于来源根目录，因此选择根 Skill，而不是只在仓库内增加 `skills/wechat-visual-director/`。

## 3. 安全边界

- Skill 和脚本不读取、写入或回显公众号 AppSecret、模型 Key、Cookie、Token。
- 创建任务默认自动规划，但不自动确认 finding、不自动选择方案、不自动创建公众号草稿。
- 相同原稿、账号和类型默认复用任务；只有明确增加 `--new-task` 才另建。
- 停止服务前校验 PID 与命令行身份；不匹配时退出码为 6，不终止未知进程。
- 图片模型缺失时允许上传、沿用或跳过，不用随机图片伪装成稿。

## 4. 本地验收

| 项目 | 结果 |
|---|---|
| Skill Creator `quick_validate.py` | 通过 |
| `scripts/install.ps1` 幂等安装 | 通过，最终输出 UTF-8 JSON |
| 包装脚本 `doctor --json` | API 与 Web 均 Ready |
| 包装脚本创建真实任务 | `status=plans_ready` |
| 自动双方案 | 通过，智能规划被调用 |
| 浏览器页面 | HTTP 200，双方案和 390px 预览加载 |
| 浏览器错误 | console/page/request failure 均为 0 |
| CLI 单元测试 | 通过 |

真实验收任务：`645fa4a7-ca6b-408b-8a5f-58ac94b23322`。

## 5. 公开发行结果

已完成：

- 公开名称冻结为 `wechat-visual-director`；
- 根目录加入 Apache License 2.0；
- 默认运行配置改为中性“示例公众号”，真实公司品牌和 CTA 移入 Git 忽略的本地私有目录；
- README 明确真实微信草稿与富文本复制属于待人工验收能力，最终群发始终由运营在公众号后台完成。

已在 `zhouke0929/wechat-visual-director` 发布 `v0.1.0-alpha.1`，并在全新 Windows + OpenCode 环境完成从 GitHub 安装、任务创建和预览验收。

## 6. 本地交付适配器验收

| 项目 | 结果 |
|---|---|
| 冻结 HTML 转 Wenyan `article.md` | 通过，资产改为交付包相对路径 |
| 交付包 | 包含 Markdown、HTML、assets、主题保护 CSS 和 manifest |
| Wenyan 2.0.11 `render` | 成功；保留 main/section/inline style/图片路径 |
| HTML 转义检查 | 未转义 |
| 非业务 table 注入 | 0 |
| 真实草稿状态 | 记录 provider、Media ID、步骤、错误和幂等键 |
| 密钥边界 | 仅进程环境或 `.env.local`；状态和前端不返回密钥值 |
| 后端回归 | 101 项通过，包含 Provider 本地配置、真实适配器成功、幂等、防重复及明确失败后重试 |
| 前端 | TypeScript 与生产构建通过 |
| Skill | UTF-8 `quick_validate.py` 通过 |

2026-07-23 已使用新注册的受控公众号完成首次真实链路验证：富文本复制可粘贴到公众号后台，Wenyan 适配器可直接创建公众号草稿，手机端预览未发现布局阻断问题。首次草稿同时暴露出导出根容器沿用了工作台 `390px` 预览宽度、米白画布底色、预览阴影和 24px 横向内边距，导致公众号电脑编辑区左右留白且手机正文边距与微信自身边距叠加；此外工作台元数据标题被一并写入正文，造成公众号标题区与正文 H1 重复。现已将发布内容改为 `width:100%`，移除根容器背景、阴影与横向内边距，并把标题预览区移到 `<main>` 正文之外；工作台和盲评页继续在外层模拟 390px 手机宽度及标题区。交付适配器也会修复旧冻结版本，复制与直接草稿两条链路均加入对应回归断言。

运营已复核修复后的电脑端宽度、手机端边距与正文标题去重，均未发现问题，本轮真实公众号闭环验收通过。后续只需单独验证包含正文图片的富文本复制稿在保存、重开后的图片持久化；该实验不阻断 Wenyan 直接草稿主链路。

## 7. AI 规划模式澄清

首次真实公众号验收时，运行状态为 `mock_text_planner / deterministic_editorial_brief`，因此该轮组件规划使用确定性规则，没有产生 Qwen 调用费用。产品架构仍保留两层智能：

- 宿主 Agent 按 Skill 把主题和资料整理成规范 Markdown；
- 核心文本规划器在配置 Qwen BYOK 后理解全文并生成受控 EditorialBrief；未配置或调用失败时回退到规则规划；
- 确定性编译器始终负责组件白名单、结构保护与微信 HTML，任何模型都不得自由输出最终 HTML/CSS。

Alpha.2 已让 API 从 Git 忽略的 `.env.local` 持久化读取文本与图片 Provider 配置，并在 `doctor` 中分别报告 `ai_text_planning` 与 `rule_text_planning`，避免把规则模式误认为真实 AI 模式。

## 8. 持久安装与升级验收

Alpha.5 把 GitHub 临时下载目录降级为安装源。安装器将程序复制到 `%LOCALAPPDATA%\wechat-visual-director\versions\<version>`，返回固定 `visual-director.ps1`，并把数据库、图片、私有配置和日志放到版本目录之外。

验收要求：

- 从任意临时目录执行安装后，稳定 launcher 位于固定目录；
- `doctor.installation.persistent=true`、`version_match=true`，且安装版本、运行版本、程序目录、数据目录可审计；
- 同版本重复安装幂等；
- 从旧版本升级后，既有任务 ID、历史窗口、图片资产和私有配置继续存在；
- 依赖安装失败时不得切换 `install.json`；
- 安装与诊断输出不得包含 AppSecret 或模型 Key。

2026-07-27 本地验收结果：

- 在隔离目录完成包含 Python 与 Web 依赖的全量安装，并由稳定 launcher 独立启动 API 与工作台；
- `doctor` 返回 `installation.version=0.1.0-alpha.5`、`persistent=true`，程序、数据、配置和运行目录均指向隔离持久目录；
- 新建真实任务后停止服务、重复安装并重新启动，原任务 ID 仍可查询；
- 重复安装前后的 SQLite 与 `.env.local` SHA-256 均保持一致；
- 排除 `.pnpm-store` 等构建缓存后，无依赖的持久复制与切换约 2.3 秒；
- 后端 108 项测试、前端 typecheck/build、Skill quick validation 全部通过。
